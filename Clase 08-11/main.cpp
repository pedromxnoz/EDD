#include <stdio.h>
#include "lista.h"

int main(){
  //Lista lista = NULL;

  /*
  if (vacia(lista)){
    printf("\nVACIA\n");
  } else{
    printf("\nNO VACIA\n");
  }

  printf("\nEl primero es: %d.", primero(lista));
  */

  /*
  inserta(10, 1, lista);
  inserta(20, 2, lista);
  inserta(30, 3, lista);
  inserta(40, 4, lista);
  inserta(70, 2, lista);

  imprimeLista(lista);

  printf("\nEl fin de la lista es: %d", fin(lista));
  printf("El dato en la posicion 3 es: %d\n", recupera(3,lista));
printf("Posicion: %d\n",localiza(40, lista));
  */

  /*
  suprime(1, lista);
  anula(lista);
  imprime_lista(lista);
  */

  return 0;
}